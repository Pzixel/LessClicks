namespace = build_railroads_events

build_railroads_events.1 = {
    type = country_event
	hidden = yes

	trigger = {
        always = yes
    }

    immediate = {
        every_scope_state = {
			create_building={
				building="building_railway"
				level=5
				reserves=1
			}
        }
        add_treasury = 88888
    }
}