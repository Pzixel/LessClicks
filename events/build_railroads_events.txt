namespace = build_railroads_events

build_railroads_events.1 = {
    type = country_event
	hidden = yes

	trigger = {
        always = yes
    }

    immediate = {
        every_scope_state = {
            add_loyalists = {
                value = 0.1
            }
        }
        add_treasury = 88888
    }
}